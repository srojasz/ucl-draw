import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import { useState } from 'react';
import Team from '../interfaces/Team.interface';
import styles from '../styles/Home.module.css'
import teamsData from '../teams.json';
import { getTwoRandomTeams } from '../utils/businessLogic';

const Home: NextPage = () => {
  const [ availableTeams, setAvailableTeams ] = useState<Team[]>(teamsData);
  const [ selectedTeams, setselectedTeams ] = useState<Team[]>([]);
  const [ rivalsPairs, setRivalsPairs ] = useState<Team[][]>([]);

  const getTwoNewTeams = () => {
    const twoTeams = getTwoRandomTeams(availableTeams);
    setselectedTeams(twoTeams);
    setRivalsPairs ([
      ...rivalsPairs,
      twoTeams
    ]);
    const newAvailableTeams = availableTeams
      .filter(team => team.id !== twoTeams[0].id && team.id !==twoTeams[1].id);
    setAvailableTeams(newAvailableTeams);
  }

  const renderTeamsInfo = (): JSX.Element => {
    return selectedTeams.length > 0 ?
    (
      <>
        <div>
          <h3>{selectedTeams[0].name}</h3>
          <p>{selectedTeams[0].country}</p>
          <p>{selectedTeams[0].group}</p>
        </div>
        <div>
          <h3>{selectedTeams[1].name}</h3>
          <p>{selectedTeams[1].country}</p>
          <p>{selectedTeams[1].group}</p>
        </div>
      </>
    )
    : <p>Not games available yet. Please, press the button to start the draw!</p>
  } 

  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className={styles.main}>
        <h1 className={styles.title}>
          Champions Draw
        </h1>
        <h2>GAMES:</h2>
        {rivalsPairs.map(rivals => {
          return <p>{rivals[0].name} VS {rivals[1].name}</p>
        })}
        <div className={styles.teams}>
          {renderTeamsInfo()}
        </div>        
        <button
          onClick={getTwoNewTeams}
          disabled={availableTeams.length === 0 && selectedTeams.length > 0}>
            NewDraw!
        </button>
      </main>

      <footer className={styles.footer}>
        <a
          href="https://vercel.com?utm_source=create-next-app&utm_medium=default-template&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          Powered by{' '}
          <span className={styles.logo}>
            <Image src="/vercel.svg" alt="Vercel Logo" width={72} height={16} />
          </span>
        </a>
      </footer>
    </div>
  )
}

export default Home
